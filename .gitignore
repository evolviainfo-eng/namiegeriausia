fs-server.js
assets/raw/
assets/qa/
assets/img/
.DS_Store

# build scripts ARE committed (gen.py / fetch_ig.py / projects.json) so the
# scheduled Instagram refresh can run in CI. Python files in a subfolder do
# not change Vercel's framework detection — only a root server file would.
assets/sheet.py
assets/build_images.py
assets/download.py
assets/fitcheck-multi.js

# workflow laukia `workflow` scope — kopija: assets/ci/instagram.yml
.github/

# Real-ESRGAN 4x masters (7 GB, regenerable: python3 assets/sr_mps.py)
assets/sr/

# Real-ESRGAN masters and the local-only tooling that makes them (needs torch).
# assets/build_ig.py IS committed: it degrades to a plain downscale without SR.
assets/sr-ig/
assets/sr_mps.py
assets/sr_fix.py
assets/used_sources.txt
